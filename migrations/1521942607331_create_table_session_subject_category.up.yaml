- args:
    sql: CREATE TABLE "session_subject_category"("id" serial NOT NULL, "session_id"
      integer NOT NULL, "subject_category_id" integer NOT NULL, PRIMARY KEY ("id")
      )
  type: run_sql
- args:
    name: session_subject_category
  type: add_existing_table_or_view
